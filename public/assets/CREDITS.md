# Asset Credits

All third-party assets here are free to use. This project is not published; assets
are used for a hobby/practice build.

## Playing cards — `cards/*.svg` (52 faces)
- Source: https://github.com/hayeah/playing-cards-assets
- Derived from Byron Knoll's "Vector Playing Cards" (public domain).
- `back.svg` is original to this project (authored here to match the table theme).

## Sound effects — `audio/*.ogg`
- Source: Kenney "Casino Audio" and "Interface Sounds" — https://kenney.nl (CC0).
- Renamed for use:
  - `deal` ← card-slide, `flip` ← card-fan, `place` ← card-place, `shuffle` ← card-shuffle
  - `chip` ← chip-lay, `chips-win` ← chips-stack
  - `click` ← click_001, `win` ← confirmation_001, `blackjack` ← glass_001
  - `lose` ← error_002, `bust` ← error_004, `push` ← minimize_001

## Table felt & chips
- Generated with CSS/SVG in the app (no external files), themed to match the table.
